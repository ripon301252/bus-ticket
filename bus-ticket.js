function busTicketA1(){
    busTicketElementById('A1');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }


    // seat name
    getTextElementValueById('seat-name');
    const seatNameIs = 'A1';
    setTextElementValueById('seat-name', seatNameIs );

    // catagory
    getTextElementValueById('catagory');
    const catagory = 'Economy';
    setTextElementValueById('catagory', catagory);

    // seat price
    getTextElementValueById('seat-price');
    const seatPrice = 550;
    setTextElementValueById('seat-price', seatPrice);
}  


function busTicketA2(){
    busTicketElementById('A2');
    
    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }


    // seat name
    getTextElementValueById('seat-name-1');
    const seatNameIs = 'A2';
    setTextElementValueById('seat-name-1', seatNameIs );

    // catagory
    getTextElementValueById('catagory-1');
    const catagory = 'Economy';
    setTextElementValueById('catagory-1', catagory);

    // seat price
    getTextElementValueById('seat-price-1');
    const seatPrice = 550;
    setTextElementValueById('seat-price-1', seatPrice);

    
}

function busTicketA3(){
    busTicketElementById('A3');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketA4(){
    busTicketElementById('A4');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

//---------------------------B-------------------------------------------
function busTicketB1(){
    busTicketElementById('B1');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}  


function busTicketB2(){
    busTicketElementById('B2');
    
    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketB3(){
    busTicketElementById('B3');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketB4(){
    busTicketElementById('B4');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

//------------------------C------------------------
function busTicketC1(){
    busTicketElementById('C1');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}  


function busTicketC2(){
    busTicketElementById('C2');
    
    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketC3(){
    busTicketElementById('C3');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketC4(){
    busTicketElementById('C4');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

//------------------------D------------------------
function busTicketD1(){
    busTicketElementById('D1');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}  


function busTicketD2(){
    busTicketElementById('D2');
    
    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketD3(){
    busTicketElementById('D3');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketD4(){
    busTicketElementById('D4');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

//----------------E--------------------------------------
function busTicketE1(){
    busTicketElementById('E1');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}  


function busTicketE2(){
    busTicketElementById('E2');
    
    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketE3(){
    busTicketElementById('E3');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketE4(){
    busTicketElementById('E4');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

//---------------F---------------------------
function busTicketF1(){
    busTicketElementById('F1');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}  


function busTicketF2(){
    busTicketElementById('F2');
    
    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketF3(){
    busTicketElementById('F3');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketF4(){
    busTicketElementById('F4');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

//--------------G-----------------------
function busTicketG1(){
    busTicketElementById('G1');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}  


function busTicketG2(){
    busTicketElementById('G2');
    
    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketG3(){
    busTicketElementById('G3');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketG4(){
    busTicketElementById('G4');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

//----------------H------------------
function busTicketH1(){
    busTicketElementById('H1');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}  


function busTicketH2(){
    busTicketElementById('H2');
    
    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketH3(){
    busTicketElementById('H3');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketH4(){
    busTicketElementById('H4');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

//-------------- I ------------------------
function busTicketI1(){
    busTicketElementById('I1');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}  


function busTicketI2(){
    busTicketElementById('I2');
    
    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketI3(){
    busTicketElementById('I3');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketI4(){
    busTicketElementById('I4');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

//---------------- J -------------
function busTicketJ1(){
    busTicketElementById('J1');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}  


function busTicketJ2(){
    busTicketElementById('J2');
    
    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }
}

function busTicketJ3(){
    busTicketElementById('J3');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }

}
      

    //---------------------
function busTicketJ4(){
    busTicketElementById('J4');

    const availableSeat = 40;
    const selectedSeat = 1;
    const noSeat = 0;
    if(availableSeat > selectedSeat){
        const seatCount = getTextElementValueById('seat-count');
        const updateSeatCount = seatCount + 1;
        setTextElementValueById('seat-count', updateSeatCount); 
    }
    else {
        console.log('Nothing')
    }

    if(selectedSeat > noSeat){
        const seatLeft = getTextElementValueById('seat-left')
        const leftSeat = seatLeft - 1;
        setTextElementValueById('seat-left', leftSeat);
    }
    else{
        console.log('Nothing')
    }

}
